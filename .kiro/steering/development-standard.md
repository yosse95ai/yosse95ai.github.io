---
inclusion: always
---
# 開発標準
以下のルールを必ず遵守すること。

## rule-1
会話セッションの最初に、必ず、Node のバージョン確認し、一致していなければ切り替えること。
1. NVMのバージョンを確認　`cat ./.nvm`
2. LocalのNodeバージョン確認 `node --version`
3. 整合していなければ、NVMのバージョンに変更する `nvm use`
